/*
import {DotAssetService} from './dot-asset.service';

describe ('DotAssetService', () => {

    const fetchMock = jest.fn();
    window.fetch = fetchMock;

    const aseetService = new DotAssetService();

    beforeEach(() => {
        fetchMock.mockReset();
        fetchMock.mockRejectedValueOnce({});
    });


    it('', () => {
        // aseetService.create()
    });

});
*/
